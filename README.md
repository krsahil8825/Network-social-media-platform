# Network

Network is a simple social media web application built with Django. It allows users to register and upload basic text-based content (posts). This repo contains the Django project in the `network/` package and a default SQLite database for local development.


